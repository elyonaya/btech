# Jour 9 : Transform

---

## Exercice 0 : Testez vos connaissances !

Répondez aux questions ci-dessous, en supprimant les réponses qui vous semblent incorrectes pour ne laissez que l'unique bonne réponse.

---

**Quelle propriété de CSS faut-il pour transformer un élément ?**

3. `transform: ...;`

---

**Quelle transformation permet de déplacer "visuellement" un élément en bas et à gauche de son point d'origine ?**

4. `translate(-100px, -100px)`

---

**Quelle transformation permet de faire tourner d'un quart vers la droite un élément ?**

2. `rotate(45deg)`

---

**Quelle transformation permet de zoomer sur un élément du double de sa taille d'origine ?**

3. `scale(2)`

---

**Quelle fonction manque à cette règle CSS ?**

```css
div {
  transform: ???(0.2);
}
```

3. `scale`

---

**Quelle valeur manque à `rotate()` ?**

```css
div {
  transform: rotate(???);
}
```

2. `7deg`

---

**Quelle valeur manque à cette règle CSS ?**

```css
div {
  transform: translate(???);
}
```

1. `40px`

---

**Quelles sont les noms des différentes fonctions de `transform` dans l'ordre indiqué ci-dessous ?**

"agrandir/réduire" ; "déplacer" ; "faire une rotation"

4. `scale()` ; `translate()` ; `rotate()`
